{
  "name": "@aggregator-dpg/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'build: web (stub)'",
    "dev": "echo 'dev: web (stub)'",
    "lint": "eslint --no-warn-ignored .",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test: web (stub)'"
  },
  "devDependencies": {
    "@aggregator-dpg/tsconfig": "workspace:*",
    "typescript": "^6.0.3"
  }
}
