# ════════════════════════════════════════════════════════════
# AGGREGATOR-DPG — environment template
#
# Usage:
#   cp infra/env.template .env
#   # populate every "change-me-*" placeholder and blank secret
#   chmod 600 .env
#   docker compose up -d --build
#
# Conventions:
#   - One file per environment (.env, .env.staging, .env.prod)
#   - Each service has a config block (non-sensitive) and a secrets block
#   - Secrets blank in template; never commit real values
#   - Generate strong secrets: `openssl rand -hex 32`
#   - On VM: chmod 600, owner root
#   - K8s migration: split blocks → ConfigMap (config) + Secret (secrets)
# ════════════════════════════════════════════════════════════


# ════════════════════════════════════════════════════════════
# postgres
# ════════════════════════════════════════════════════════════
# --- config ---
POSTGRES_USER=aggregator
POSTGRES_DB=aggregator
# --- secrets ---
POSTGRES_PASSWORD=change-me-postgres-password


# ════════════════════════════════════════════════════════════
# redis
# ════════════════════════════════════════════════════════════
# --- config ---
REDIS_URL=redis://redis:6379
# --- secrets ---
# (none — internal docker network only)


# ════════════════════════════════════════════════════════════
# minio
# ════════════════════════════════════════════════════════════
# --- config ---
MINIO_ROOT_USER=minioadmin
# --- secrets ---
MINIO_ROOT_PASSWORD=change-me-minio-password


# ════════════════════════════════════════════════════════════
# public-facing endpoint composition
# ════════════════════════════════════════════════════════════
# All browser-visible URLs are composed in docker-compose.yml from
# these five vars. Change PUBLIC_HOST to point the whole stack at a
# different hostname (laptop: `localhost`, VM: the VM's public IP,
# staging: a real DNS name).
PUBLIC_SCHEME=http
PUBLIC_HOST=localhost
PORTAL_PORT=3100
API_PORT=4000
KC_PORT=8080


# ════════════════════════════════════════════════════════════
# keycloak
# ════════════════════════════════════════════════════════════
# --- config ---
KC_BOOTSTRAP_ADMIN_USERNAME=admin
KC_SPI_SMS_PROVIDER=log
AWS_REGION=ap-south-1
# --- secrets ---
KC_BOOTSTRAP_ADMIN_PASSWORD=change-me-keycloak-admin-password
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=


# ════════════════════════════════════════════════════════════
# mailhog / smtp (shared by api + keycloak)
# ════════════════════════════════════════════════════════════
# --- config ---
MAIL_PROVIDER=smtp
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM=no-reply@bluedots.local
SMTP_SECURE=false
SES_FROM=
SES_CONFIGURATION_SET=
# --- secrets ---
SMTP_USER=
SMTP_PASSWORD=


# ════════════════════════════════════════════════════════════
# api
# ════════════════════════════════════════════════════════════
# CORS_ORIGINS, PUBLIC_API_URL, PUBLIC_PORTAL_URL, KEYCLOAK_URL all
# composed in docker-compose.yml from PUBLIC_*/PORT vars above.
# --- config ---
LOG_LEVEL=info
RUN_MIGRATIONS_ON_BOOT=true
KEYCLOAK_REALM=aggregator
KEYCLOAK_ADMIN_CLIENT_ID=aggregator-api
ADMIN_EMAILS=admin@bluedots.local
# --- secrets ---
KEYCLOAK_ADMIN_CLIENT_SECRET=change-me-keycloak-api-service-secret
APPROVAL_TOKEN_SECRET=                  # openssl rand -hex 32   (≥32 chars, REQUIRED)


# ════════════════════════════════════════════════════════════
# web
# ════════════════════════════════════════════════════════════
# NEXT_PUBLIC_API_URL, OIDC_ISSUER, OIDC_REDIRECT_URI, OIDC_POST_LOGOUT_REDIRECT_URI,
# PUBLIC_PORTAL_URL all composed in docker-compose.yml from PUBLIC_*/PORT vars above.
# --- config ---
API_BASE_URL=http://api:4000           # docker-network DNS; not browser-visible
OIDC_CLIENT_ID=aggregator-portal
OIDC_SCOPE=openid profile email
SESSION_COOKIE_NAME=sid
SESSION_TTL_SECONDS=43200
COOKIE_SECURE=                         # leave blank for prod (HTTPS); `false` for HTTP dev
# --- secrets ---
OIDC_CLIENT_SECRET=change-me-keycloak-portal-client-secret
SESSION_KEY=                            # openssl rand -hex 32   (≥32 chars, REQUIRED)
