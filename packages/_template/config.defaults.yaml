# Default configuration values for this package.
#
# Loaded at startup via @aggregator-dpg/config-loader (wired in a future feature).
# Per-environment overrides live in config/env/{dev,staging,prod}.yaml.
#
# Add this package's defaults here, nested under the package configKey.
#
# template:
#   exampleTimeoutMs: 5000
#   exampleBaseUrl: "https://example.internal"
